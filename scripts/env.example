# subscription-auth/scripts — local secrets (copy to .env.local, never commit)
SUBSCRIPTION_AUTH_BASE_URL=https://preview.auth.ipay.sh
SUBSCRIPTION_AUTH_ISS=https://preview.auth.ipay.sh
SUBSCRIPTION_AUTH_SERVICE_ID=e2e.local.ipay.sh
SUBSCRIPTION_AUTH_SERVICE_URL=http://127.0.0.1:3000

# Path to Solana keypair JSON (64-byte array) for merchant / register + issue
MERCHANT_KEYPAIR=../../demo-wallets/seller-keypair.json

# Test payer wallet (buyer pubkey) for auth-only issue flow
TEST_PAYER=buyA5hR1Z9KtHQRBTmLkjsFfjAabDwdZtrRC6edqxAJ
